// Material Import
import Stack, { StackProps } from "@mui/material/Stack";

const RevediaStack = (props: StackProps) => {
    return <Stack {...props} />
};

export default RevediaStack; // Wrapper Component Over Stack